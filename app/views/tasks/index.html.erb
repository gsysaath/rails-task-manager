<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id)  %> Done?: <%= task.completed %></li>
  <% end %>
</ul>

<%= link_to "Create a new Task", tasks_new_path %>
